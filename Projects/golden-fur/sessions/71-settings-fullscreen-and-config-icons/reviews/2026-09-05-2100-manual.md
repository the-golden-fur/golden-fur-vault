---
title: Code review — feat/settings-fullscreen-config-icons (manual)
date: 2026-09-05
tags: [code-review, golden-fur]
project: golden-fur
---

# Code review — `feat/settings-fullscreen-config-icons`

- **Trigger:** manual (follow-up pass after 2026-09-05-2006-manual.md, APPROVE WITH NITS, 0 blocking / 4 non-blocking)
- **Base:** `dev` (merge-base `61ea5fa`, i.e. current `dev` tip — branch still has no commits of its own, all changes are staged/uncommitted)
- **Reviewed:** staged working-tree diff (`git diff --cached`) against `dev`
- **Commits:** 0 — all twelve files staged but uncommitted
- **Diffstat:** 12 files changed, +367 / −291 (3 new files, 9 modified)
- **Reviewer:** code-reviewer subagent — read-only, did not author the change

## Verdict: APPROVE WITH NITS

This is a follow-up pass verifying the four non-blocking findings from the prior review were actually addressed, plus checking for anything the fixes themselves introduced. All four are fixed correctly:

1. **N1 (unmemoized context value)** — fixed. `client/src/shared/providers/SidebarCollapseProvider/SidebarCollapseProvider.tsx` is a real component now; it wraps `{ collapsed, setCollapsed }` in `useMemo([collapsed, setCollapsed])`, and `AppShell.tsx` renders `<SidebarCollapseProvider collapsed={collapsed} setCollapsed={setCollapsedPersist}>` around its whole subtree instead of an inline `<Context.Provider value={{...}}>`. Matches the `ThemeProvider` shape exactly.
2. **N2 (folder held no Provider component)** — fixed by the same file; the `SidebarCollapseProvider/` folder now contains both `SidebarCollapseProvider.tsx` and `sidebarCollapseContext.ts`, matching `ThemeProvider/`/`ToastProvider/`.
3. **N3 (unindented `.map()` JSX body)** — fixed. `SettingsPage.tsx:565-610` now has the `const TileIcon = tile.icon; return (...)` block body correctly indented one level deeper than `return (`, matching Prettier's normal output. Spot-checked every line in the block (and the surrounding `orderedTabs.map` for comparison) for >80-char overflow: the only line over 80 (`SettingsPage.tsx:575`, 88 chars, the `sidebarSubitem`/`sidebarItemActive` ternary template literal) is not new — the identical line already existed in `dev` at 86 chars before this branch touched it (confirmed via `git show dev:...`), i.e. Prettier already tolerated it there because it can't break inside a template-literal token; the 2-char growth is just the one extra indent level from the new block scope. Not a fix regression.
4. **N4 (stale "Fullscreen toggle" doc comment)** — fixed in `configTiles.config.ts:31-35`, now correctly says "Open as a full page" and drops the "resizing the panel" reference. **However**, the same kind of staleness still exists one file over: `client/src/features/staff/components/dashboard/DashboardTile/DashboardTile.tsx:7-13`'s `onSelect` doc comment still says "used inside the **Settings modal**... if the modal is **later expanded to fullscreen**." This exact comment predates this branch (verified unchanged in `git show dev:...DashboardTile.tsx`), so it isn't something this branch's diff touches directly, but the branch is precisely the one that deletes the modal/fullscreen concept from `SettingsPage`, and the sibling fix (N4) already established that this terminology should be cleaned up — this instance was simply missed. See N4-cont below.

Test coverage (previously flagged as the only unaddressed gap of substance): the two new `AppShell.spec.ts` tests use a `SidebarCollapseProbe` that reproduces `SettingsPage`'s exact mount/unmount pattern (`useRef` capturing the pre-existing `collapsed` value, force-collapse on mount, restore on unmount) rendered as a real sibling route under the same `AppShell`, not a shallow mock. Traced both: (a) force-collapse + restore-to-uncollapsed round-trip, and (b) force-collapse + restore-to-a-_pre-existing-collapsed_ value (seeded via `localStorage.setItem('sidebar-collapsed-staff', 'true')` before render) — both assert against the real `Sidebar`'s "Collapse sidebar"/"Expand sidebar" button labels, which is the actual DOM signal the previous review said was missing coverage for. This closes the gap correctly.

No new bugs introduced by any of the four fixes. Safe to proceed; only a very small residual nit remains (N4-cont, essentially the same class of finding as before, just in the neighboring file).

## Non-blocking findings

### N4-cont. Stale "Settings modal"/"fullscreen" doc comment survives in the neighboring file — `client/src/features/staff/components/dashboard/DashboardTile/DashboardTile.tsx:7-13`

The `onSelect` prop's doc comment: `"...used inside the Settings modal, where selecting a Config tile embeds the target page inline rather than leaving the modal. `to` still comes along on the same tile config so the caller knows what to navigate to if the modal is later expanded to fullscreen."` Both "Settings modal" and "expanded to fullscreen" describe concepts this branch removes entirely (no modal, no fullscreen toggle — just a full-bleed page and an "Open as a full page" button). This comment predates the branch and the diff doesn't touch it, but it's the same terminology the N4 fix in `configTiles.config.ts` already cleaned up one file over, so it reads as an oversight rather than genuinely out of scope. Reword alongside the new `icon` doc comment already added two lines below it in the same interface.

## Test coverage

- No new gaps found beyond what was already noted in the prior review (`DashboardTile.spec.ts`/`ConfigTab.spec.ts` still don't assert the icon actually renders — low-value, decorative-prop gap, unchanged from before).
- The two new `AppShell.spec.ts` tests correctly close the previously-flagged sidebar-collapse-round-trip gap; verified their logic by tracing collapsed-state transitions against `Sidebar`'s real aria-labels rather than just reading the assertions at face value.
- Suites were not run as part of this review (that is `ci-verifier`'s job).

## Files reviewed

- `client/src/shared/providers/SidebarCollapseProvider/SidebarCollapseProvider.tsx` (new) — real memoized Provider component, closes N1/N2.
- `client/src/shared/providers/SidebarCollapseProvider/sidebarCollapseContext.ts`, `client/src/shared/hooks/useSidebarCollapse/useSidebarCollapse.ts` — unchanged since prior review, still consistent with sibling conventions.
- `client/src/shared/components/AppShell/AppShell.tsx` — now renders `<SidebarCollapseProvider>` instead of the inline unmemoized `<Context.Provider>`.
- `client/src/shared/components/AppShell/AppShell.spec.ts` — two new tests via a `SidebarCollapseProbe` component, closes the test-coverage gap.
- `client/src/pages/SettingsPage/SettingsPage.tsx` — `.map()` indentation fixed (N3); doc comment reworded to drop stale fullscreen/resize language (part of the overall diff, not itself a new fix target).
- `client/src/pages/SettingsPage/configTiles.config.ts` — doc comment reworded, closes N4 for this file.
- `client/src/features/staff/components/dashboard/DashboardTile/DashboardTile.tsx` — unchanged doc-comment staleness noted as N4-cont; new `icon` prop itself unchanged since prior review and still correct.
- `client/src/pages/SettingsPage/SettingsPage.spec.ts`, `configTiles.config.ts`, `tabs/ConfigTab.tsx`, `DashboardTile.module.css`, `SettingsPage.module.css` — unchanged in substance since prior review; re-read in full, no new issues.
