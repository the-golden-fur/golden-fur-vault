# Context — 68-service-type-staff-roles

## Copied into ./context/

Nothing — the two sources this session used are handled below (one is
project-wide canonical material already in the vault; the other has no
source file to copy).

## Referenced only (not copied)

- `golden-fur-vault/Projects/golden-fur/shared/context/architecture/Architectural-Change-History.docx`
  — the backlog row (assignee Matthew, status "Not Started") this session
  addresses:

  > When creating new service type > staff picker, it doesn't specify what
  > staff roles are available to choose from. Extend it by adding options to
  > choose staff roles (multi-select). Since grooming services > groomer,
  > vet services > vets, etc. Do the same for existing service types and
  > update seed scripts. Also remove the key field, this should be randomly
  > generated anyway, only leave out name. Just drop key field, it seems
  > redundant, I don't like it showing in the list either.

  Project-wide canonical material already tracked in the vault at that path
  — not session-specific, so not copied here (see the sessions skill's
  "Project-wide reference material" note).

## No source file — mid-session chat follow-up

The second, related instruction that shaped this session (removing the
Admin Settings > Policies Staff Picker toggle) did not come from any
document — it arrived verbatim in this chat thread, mid-session, after the
plan for the first request was already in flight. Quoted here in full since
there is nothing to point to:

> while you're at it, I think there's a stray staff picker config at admin
> settings > policies, even though it can already be configured now at
> service type. get rid of it.

## Secrets

None involved in this session.
