# Context — 86-promo-variations-and-spin-wheel

## Copied into ./context/

- None — the only context used was the shared Architectural-Change-History
  log, which stays canonical (see below).

## Referenced only (not copied)

- `Projects/golden-fur/shared/context/Architectural-Change-History.docx` —
  "In Progress" items (owned by Matthew) that are this session's entire
  scope: "Include variations to promos... Add new promo type... Add new
  promo step in booking process" plus the coupon spin wheel user stories
  (as a customer, earn a random %/flat PHP coupon via a spin wheel after X
  bookings/spend; as an admin, configure the reward pool, rarity %, and
  pity requirement). Project-wide reference material, not session-specific
  — referenced in place rather than copied.
