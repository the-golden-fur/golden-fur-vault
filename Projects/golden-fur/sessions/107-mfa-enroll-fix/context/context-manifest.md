# Context — 107-mfa-enroll-fix

## Copied into ./context/

None — this session was pure conversational debugging plus live queries against the dev Supabase project, with no pasted brief or external document to preserve.

## Referenced only (not copied)

- `golden-fur/server/.env` — `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` used to run one-off diagnostic scripts (checking `mfa_lockouts`, TOTP factor state, resetting the lockout) directly against the dev project (`hikgijuipymfghfuyrjv`); a secrets file, never copied.
