# Context — 114-spin-wheel-promo-triggers-and-pools

## Copied into ./context/

None. The only context material this session used was one text-only row
inside a project-wide, multi-topic reference PDF — see below for why it was
referenced rather than copied.

## Referenced only (not copied)

- `Projects/golden-fur/shared/context/Architectural-Change-History.pdf`,
  **page 8** — this is canonical, cross-session reference material (a
  running log of requests, most of it unrelated to this session), so per the
  session-documentation convention it stays in `shared/context/` rather than
  being copied wholesale here. Page 8 is a single "In Progress" row assigned
  to Matthew, containing the six bullet points quoted verbatim in `plan.md`'s
  "What you asked for" and this record's "The request, verbatim" — there are
  no screenshots on this page (unlike, e.g., session 113's source page),
  just the request text itself.

- `golden-fur/server/.env` (and `golden-fur/client/.env`, if present) — not
  read or needed for this session; the manual test steps and Postman
  collection above use seeded staff/customer accounts (e.g. `makati.admin1`,
  `customer1@goldenfur.com`) and collection variables rather than any secret
  value. Listed here only per the session-documentation convention's
  standing reminder that a `.env*` file is never copied.
