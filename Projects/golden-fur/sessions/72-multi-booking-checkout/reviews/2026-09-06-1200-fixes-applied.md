# Fix status — findings from `2026-09-06-1059-pre-pr.md`

The `code-reviewer` subagent's pass (`2026-09-06-1059-pre-pr.md`, verdict
CHANGES REQUESTED, 6 findings) completed and wrote its report before hitting
a session rate limit. Status of each finding, given the remaining session
budget:

- **#3** (bulk `payment_status` update overwrites Cancelled/No-show
  siblings) — **fixed**. `recomputeBookingGroupPaymentStatus` now excludes
  `Cancelled`/`No-show` member bookings from both the bulk mirror update and
  the `previouslyPending` side-effects loop
  (`server/src/features/booking/services/booking.service.ts`).
- **#5** (client discount-scope filter doesn't match the server's
  "exactly one category" rule) — **fixed**. `applicableDiscounts` now
  requires `groupCategories.size === 1` before matching a category-scoped
  discount, mirroring `bookingGroup.service.ts`'s `uniformCategory`
  (`CustomerBookingFlowPage.tsx`).
- **#1** (cancelling a grouped booking loses its credit —
  `confirmedAmountPaid` queries by `booking_id` only), **#2** (one Cancelled
  sibling permanently blocks the whole group's cashier checkout), **#4**
  (a Veterinary sub-booking is charged upfront when bundled with a
  non-Veterinary one, unlike a standalone Veterinary booking), and **#6**
  (a Walk-in sub-booking bundled with an Online one loses its own AC-5
  post-insert capacity re-check and gets swept into the group's downpayment
  gate) — **not fixed in this session**, due to the session budget
  constraint the user flagged mid-session. Each needs a real, independently
  verified fix (cancellation-credit attribution, checkout-preview filtering,
  charge-splitting by category, and per-sub-booking capacity/downpayment
  gating respectively) rather than a rushed edit to money/capacity-critical
  code with no room left to re-verify it properly.

**These four remain genuine BLOCKING findings against merging this PR to
`dev`** per this repo's own code-review policy — the PR is opened for
visibility and to keep the branch moving, not because it is judged
merge-ready. Fix directions are detailed in the original report.
