# Pre-PR review — 74-seeds-module-dirs

- **Date:** 2026-09-06 15:30
- **Trigger:** pre-pr
- **Method:** in-session review (`code-review` skill, no subagent) over the
  `chore/seeds-module-dirs` diff vs `dev`.
- **Verdict:** PASS — 0 blocking, 0 non-blocking findings.

## What was checked

1. **Split correctness** — every function from the two split source files
   lands in exactly one destination:
   - `module-3` → `seedServiceBranchAvailability`, `seedGoldenPackage` in
     `m13`; `seedMandatedDiscounts` in `m12`. `SERVICE_CATEGORIES` /
     `MANDATED_DISCOUNTS` consts moved with the discount function.
   - `module-5` → `seedPromos` (+ `PROMO_SEEDS`, `PromoSeed`) in `m13`;
     `resolveVeterinarianId` + the two catalog seeders (+ their `*_SEEDS`
     consts) in `m07`.
   - `m07` no longer imports/uses `getBranches` (vet catalog isn't
     branch-scoped) — correctly dropped.
   - `m13.main()` runs service-avail → package → promos; `m12.main()` runs
     discounts; `m07.main()` runs vet catalogs. No function orphaned or
     duplicated.
2. **SQL mirrors** — `m13.seed.sql` = old §1 + §2 + the promos block from
   `module-5.seed.sql`; `m12.seed.sql` = old §3 (discounts); `m07.seed.sql`
   = old vet-catalog block. All still `where not exists` / `on conflict do
nothing` guarded. Plain SQL, no migration-function calls.
3. **Spec coverage** — each new `.spec.ts` keeps the mock tables and the
   "creates …" + "is idempotent" cases for its own functions. `m13` spec
   generalised the promo branch-availability assertion from a hard-coded
   `* 2` to `* branches.length`. `npm run test:seed`: 6 files / 25 tests
   green.
4. **Run order** — new `sql_paths` / `seed:all` order is `m01, m02, m05,
m07, m12, m13`. Every folder except `m01`/`m02` only depends on
   `branches` / `staff_profiles` (m01) or `customer_profiles` (m02), which
   run first. No seed-to-seed dependency among m05/m07/m12/m13. `m13` also
   needs the migration-seeded base service catalog (20260715034), which runs
   before any seed.
5. **Idempotency preserved** — no insert lost its existence check in the
   move; `test:seed`'s idempotency cases pass.
6. **Blast radius** — `supabase/seeds/` is dev tooling, not imported by
   `client/` or `server/` builds or their test suites. `format:check`
   clean. Applied migrations referencing the old folder names in comments
   left untouched (correct — never edit an applied migration).

## Findings

None. One note for the PR body: applied-migration comments still say
`module-3-maintenance/` etc.; those are frozen history and were
deliberately not touched.

## Not covered

A live `supabase db reset` + `seed:all` against a real database — not run
here to avoid disturbing the dev DB. The `.sql` files are straight
re-partitions of SQL that already ran green, and `test:seed` covers the
`.ts` path.
