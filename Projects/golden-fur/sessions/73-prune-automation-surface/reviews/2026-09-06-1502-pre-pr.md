# Pre-PR review — 73-prune-automation-surface

- **Date:** 2026-09-06 15:02
- **Trigger:** pre-pr
- **Method:** in-session review (per the new pipeline — `code-review` skill,
  no subagent). Scope: `dev...HEAD` in golden-fur + the vault branch diff.
- **Verdict:** PASS — 0 blocking, 0 non-blocking findings actioned during
  review.

## Diff shape

Tooling-only. Every changed file is `.md` (skills, `AGENTS.md`, session
docs), `.sh` (2 hooks per repo), `.json` (`.claude/settings.json`), or
`.toml` (deleted `.gemini` adapters). Zero files under `client/`,
`server/`, `supabase/`. No runtime behaviour of the golden-fur app can
change.

## What was checked

1. **Dangling references** — grep for all 15 deleted skill/agent/hook names
   across `.agent/`, `.claude/`, `.gemini/`, `.codex/`, and both `AGENTS.md`.
   Only intentional "retired 2026-09-06" notes, the unchanged ADR path, and
   frozen `sessions/**` records remain. ✅
2. **Hook scripts** — `bash -n` syntax check on all four (both
   `session-router.sh`, both `pr-guard.sh`). ✅ `pr-guard.sh` logic is
   unchanged (wording only); `session-router.sh` changes are limited to the
   emitted `additionalContext` string. Smoke-tested: correct pipeline text
   on an "open a PR" prompt, silent on an ordinary prompt, `pr-guard` still
   denies `gh pr create` without evidence.
3. **`.claude/settings.json`** — valid JSON in both repos after the `Stop`
   block removal; `UserPromptSubmit` + `PreToolUse` blocks intact. ✅
4. **Pipeline consistency** — `pr-to-dev.md`, `pr-dev-to-main.md`, vault
   `pr.md`, both `session-router.sh` finish blocks, and both `AGENTS.md`
   now describe the same pipeline (ci-verifier once → ci-fixer if red →
   in-session `code-review` → folder check → commit → PR). Step numbers
   renumbered consistently after removing the workflow-doc-sync step. ✅
5. **`pr-guard` still enforced** — the gate (ci-verifier marker + a
   `reviews/` file) is unchanged; this review file + the marker are what
   satisfy it for this PR.
6. **Prettier / ESLint / tsc** — see `testing/testing.md`. All green.

## Findings

None blocking. One note carried into the PR body: the dormant-module and
gardening agents are recoverable from git history (`git show <sha>:<path>`)
if a module goes active or the vault workflow needs one back.

## Not covered

Full Vitest suites + client build — structurally unaffected by this diff;
GitHub Actions runs them on the PR.
