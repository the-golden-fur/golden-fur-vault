---
title: Release promotion dev → main (golden-fur PR #141)
date: 2026-09-02
tags: [release, promotion, deployment, vercel, render, golden-fur]
project: golden-fur
---

Source request (Matthew, 2026-09-02): open a PR from the golden-fur `dev`
branch to `main` so the deployed Vercel (client) and Render (server) pick up
the latest changes from `dev`.

## PR

- **golden-fur#141** — `release: promote dev → main (#127–#140)`
- Base `main` @ `a79436f` (last promotion was PR #126) → head `dev` @
  `d45108f`, 14 commits, `MERGEABLE`.
- Merge strategy: **rebase preferred, plain merge as fallback, never
  squash** (per `pr-dev-to-main` skill).
- Not yet merged — opened only; merge is a separate explicit step.

## What ships when this merges

Everything merged into `dev` since promotion #126:

| Area               | PRs                                                                                                                                                                                                                                                                         |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Payments & credits | #135 transactions model rework · #136 gaps A–D + navbar credit + dev-env/CI cleanup · #138 customer credit-expiry view + config modes · #139 payment scheme choice + partial settlement                                                                                     |
| Booking            | #128 show new service types in flow · #129 notice period as lead-time floor · #130 explicit cancel-confirm modal · #132 disclose non-refundable-to-credit policy · #134 Service Type info popover · #140 separate online-booking notice period + slot reset on future dates |
| UI                 | #133 remove oversized gaps, fix control sizing                                                                                                                                                                                                                              |
| Tooling / infra    | #127 code-reviewer + ci-verifier subagents · #131 module-5 seed + maintenance agents/skills · #137 deterministic session/PR hooks + finish-pipeline rework                                                                                                                  |

## Verification

- `ci-verifier` marker green for `dev` HEAD `d45108f`. The tree at
  `d45108f` is byte-identical (`0268b1f…`) to the PR #140 feature-branch
  tree verified at merge time, so CI parity across `golden-fur` +
  `golden-fur-vault` carries over without a re-run.
- Each feature/fix branch was `code-reviewer`-reviewed at its own
  `pr-to-dev` time — reports under
  `Projects/golden-fur/sessions/<NN-slug>/reviews/`. A `dev` → `main`
  promotion adds no new code, so it needs no fresh full-tree review.

## Follow-up

- Merge PR #141 (rebase) when ready → triggers Vercel + Render redeploys
  off `main`.
- Confirm any new env vars introduced since #126 (payment scheme, credit
  config modes) are set on the Vercel/Render dashboards before or right
  after merge — see
  [[2026-08-30-production-deployment-hardening]] and the deploy-config
  runbook.
