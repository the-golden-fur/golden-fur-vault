---
title: Release promotion dev → main (golden-fur PR #146)
date: 2026-09-05
tags: [release, promotion, deployment, render, golden-fur]
project: golden-fur
---

Source request (Matthew, 2026-09-05): open a PR from the golden-fur `dev`
branch to `main` so the deployed Render (server) picks up the latest
changes from `dev`.

## PR

- **golden-fur#146** — `release: staff-role multi-select on Service Types + server esbuild build step`
- Base `main` @ `a0523a5` (last promotion was PR #142) → head `dev` @
  `61ea5fa`.
- Merge strategy: **rebase preferred, plain merge as fallback, never
  squash** (per `pr-dev-to-main` skill).
- Not yet merged — opened only; merge is a separate explicit step.

## What ships when this merges

Everything merged into `dev` since promotion #142 (51 files changed,
+1934/−417 by direct tree diff `main...dev`):

| Area             | PRs                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Maintenance / UI | #143 configurable staff-role eligibility on Service Types (multi-select, replaces hardcoded Grooming/Veterinary maps), drops redundant `policy_configurations` Staff Picker toggle, removes client-supplied Service Types `key` field, seeds missing `Assessment` service type, fixes a `resolveStaffAssignment` bug that dropped staff preference outside Grooming/Veterinary |
| Tooling / infra  | #145 server production build via `esbuild` (bundles `src/app.ts` → `dist/app.js` once at deploy time instead of `tsx` transpiling on every boot), local `dist/` cleanup scripts, new `build-server` CI check                                                                                                                                                                   |

## Verification

- `ci-verifier` marker green for `dev` HEAD `61ea5fa` — all 7 checks
  (client/server tests, lint, format, client/server build) passed fresh
  against `dev` right before opening this PR.
- Each feature/fix branch was `code-reviewer`-reviewed at its own
  `pr-to-dev` time — reports under
  `Projects/golden-fur/sessions/68-service-type-staff-roles/reviews/` and
  `Projects/golden-fur/sessions/70-server-build-step-and-dist-clean/reviews/`.
  #143's blocking finding (missing Postgres grants on
  `get_staff_availability`) was fixed by a follow-up migration
  (`20260904172_m03_get_staff_availability_grants_fix.sql`) already in
  `dev`. #145's blocking finding (no CI coverage for the new `esbuild`
  build step) was fixed by adding the `build-server` CI job before merging
  #145 into `dev`. A `dev` → `main` promotion adds no new code, so it needs
  no fresh full-tree review.

## Follow-up

- Merge PR #146 (rebase) when ready → triggers the Render redeploy off
  `main`. (No `VITE_*`/client-facing change in this promotion, so no
  Vercel env-var follow-up is needed this time.)
- **Manually update the Render dashboard's Build Command** (Settings →
  golden-fur-server → Build) from `npm install` to `npm ci && npm run
build`, matching `render.yaml` — this isn't auto-applied to the existing
  service, and without it the server would still deploy fine (falling back
  to whatever was last built) but never actually pick up the new `esbuild`
  build step. See
  `Projects/golden-fur/sessions/70-server-build-step-and-dist-clean/`.
