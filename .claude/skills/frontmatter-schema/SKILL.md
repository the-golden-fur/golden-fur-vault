---
name: frontmatter-schema
description: Canonical YAML frontmatter fields (title, date, tags, project, plus optional type/source/status) used across this vault. Use whenever writing or checking a note's frontmatter, or when unsure what field to use for a note's kind or citation.
---

# Frontmatter schema

This is the Claude Code adapter for a tool-agnostic skill. Read and follow
the full instructions at
[.agent/skills/frontmatter-schema.md](../../../.agent/skills/frontmatter-schema.md)
before proceeding — that file is the canonical, maintained version (also
used by other AI coding tools working in this repo); this file exists only
so Claude Code can discover and auto-invoke it.
