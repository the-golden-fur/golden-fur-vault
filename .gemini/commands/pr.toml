description = "Open a draft PR in this vault targeting main (merge commit only)"
prompt = """
Read and follow the instructions in .agent/skills/pr.md in this repo, then
open the PR described below - as a draft, with every field set, and no
CI/format/review step. Target main, merge commit only.

{{args}}
"""
