description = "Open a PR in this vault targeting main (merge commit only)"
prompt = """
Read and follow the instructions in .agent/skills/pr.md in this repo, then
open the PR described below. Target main, merge commit only.

{{args}}
"""
