description = "Merge a PR in this vault into main (merge commit only, confirms first)"
prompt = """
Read and follow the instructions in .agent/skills/merge-pr.md in this repo,
then merge the PR described below. Confirm readiness and get explicit
go-ahead before merging; merge commit only.

{{args}}
"""
